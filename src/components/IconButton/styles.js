import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  active: {
    position: 'absolute',
    top: 0,
    right: 0,
    alignItems: 'center',
    justifyContent: 'center',
    width: 15,
    height: 15,
    borderRadius: 10,
    backgroundColor: '#20983e',
  },
  activeIcon: {},
});

export default styles;
